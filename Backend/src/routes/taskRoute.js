// const express = require("express");
// const router = express.Router();
// const roles = require("../constants/roles");
// const requireAuth = require("../middleware/requireAuth");
// const apiController = require("../controller/apis");

// router.get("/tasks/:id", requireAuth(roles), authorController.getById);

// router.get("/tasks", requireAuth(roles), apiController.getAllTasks);

// router.put("/tasks/:id", requireAuth(roles), authorController.update);

// router.post("/tasks", requireAuth(roles), authorController.create);

// module.exports = router;
